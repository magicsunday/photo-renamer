services:
    buildbox:
        build: .
        volumes:
            - .:/app
            - ~/.ssh:/root/.ssh:ro
        environment:
            - COMPOSER_AUTH

    builder:
        build:
            context: .build/docker
            dockerfile: Dockerfile
            args:
                USERID: ${USERID}
                GROUPID: ${GROUPID}
        volumes:
            - ./:/renamer
        working_dir: /renamer
        user: "renamer"
